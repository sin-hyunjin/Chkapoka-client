import { Meta, Title, IconGallery, IconItem } from "@storybook/addon-docs";
import { IconExample } from "@/../.storybook/IconExample";
import CpLogo from "@/components/commons/images/CpLogo.vue";
import IconAlert from "@/components/commons/images/IconAlert.vue";
import IconArrowLeft from "@/components/commons/images/IconArrowLeft.vue";
import IconPlus from "@/components/commons/images/IconPlus.vue";
import IconInvite from "@/components/commons/images/IconInvite.vue";
import IconAddPost from "@/components/commons/images/IconAddPost.vue";
import Iconshare from "@/components/commons/images/Iconshare.vue";
import IconEditButton from "@/components/commons/images/IconEditButton.vue";
import IconEditPen from "@/components/commons/images/IconEditPen.vue";
import IconRefresh from "@/components/commons/images/IconRefresh.vue";
import IconTree from "@/components/commons/images/IconTree.vue";
import IconFlower from "@/components/commons/images/IconFlower.vue";

<Meta title="Iconography" />

export const iconList = [
  CpLogo,
  [IconAlert, { fillColor: "var(--cp-color-red)" }],
  [IconArrowLeft, { fillColor: "var(--cp-color-black)" }],
  [IconPlus, { fillColor: "var(--cp-color-red)" }],
  [Iconshare, { fillColor: "var(--cp-color-red)" }],
  [IconInvite, { fillColor: "var(--cp-color-red)" }],
  [IconAddPost, { fillColor: "var(--cp-color-red)" }],
  [IconEditButton, { fillColor: "var(--cp-color-gray-100)" }],
  [IconEditPen, { fillColor: "var(--cp-color-gray-100)" }],
  [IconRefresh, { fillColor: "var(--cp-color-gray-100)" }],
  IconTree,
  IconFlower,
];

# Iconography

<IconGallery>
  {iconList.map((v) => {
    if (Array.isArray(v)) {
      const [component, prop] = v;
      return (
        <IconItem name={component.name} key={component.name}>
          <IconExample component={component} prop={props} />
        </IconItem>
      );
    } else {
      return (
        <IconItem name={v.name} key={v.name}>
          <IconExample component={v} />
        </IconItem>
      );
    }
  })}
</IconGallery>
